# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    container:
      image:  google/dart:latest

    steps:
      - uses: actions/checkout@v2
      - name: basic settings
        run: |
          apt update 
          apt-get install curl -y
          apt-get install git -y
          apt-get install unzip -y
          apt-get install zip -y
          apt-get install xz-utils
      - name: Mount flutter
        run: git clone -b master https://github.com/flutter/flutter.git
      - name: Show version
        run: ./flutter/bin/flutter --version
      - name: Install dependencies
        run: flutter/bin/flutter packages get
      - name: Enable web app
        run: flutter/bin/flutter config --enable-web
#      - name: Run tests
#        run: flutter/bin/flutter test
      - name: Build app
        run: flutter/bin/flutter build web
      - name: deploy hosting on firebase
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}


#name: Deploy to Firebase Hosting on merge
#'on':
#  push:
#    branches:
#      - main
#jobs:
#  build_and_deploy:
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v2
#      - run: npm ci && npm run build
#      - uses: FirebaseExtended/action-hosting-deploy@v0
#        with:
#          repoToken: '${{ secrets.GITHUB_TOKEN }}'
#          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_TESTE_SITE_FDB2F }}'
#          channelId: live
#          projectId: teste-site-fdb2f
